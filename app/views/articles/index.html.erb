<h1> ARTICLES </h1>

<table>
<tr>
    <th> Title </th>
     <th> Text </th>
</tr>
    <td> </td>
<tr>
</tr>

<% @articles.each do | article | %>
    </tr>
        <td> <%= article.title %> </td>
        <td> <%= article.text %> </td>
        <td> <%= link_to 'show', article_path(article) %> </td>
    <tr>
<% end %>
</table>